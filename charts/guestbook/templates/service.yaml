apiVersion: v1
kind: Service
metadata:
  name: guestbook-ui
  labels:
    app.kubernetes.io/name: guestbook-ui
spec:
  type: {{ .Values.service.type }}
  selector:
    app.kubernetes.io/name: guestbook-ui
  ports:
    - name: http
      port: {{ .Values.service.port }}
      targetPort: http
